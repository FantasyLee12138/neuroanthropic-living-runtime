
# 类脑活人架构 v0.4
## CLI / Always-on / Multi-Agent / Probability-Driven Runtime

---

## 0. v0.4 更新摘要

v0.4 在 v0.3 的基础上，继续把系统从“概率驱动的多 Agent 活人原型”升级为“**可调度、可解释、可容错、可资源约束**”的 runtime 方案。

相较 v0.3，v0.4 的核心新增内容：

1. 新增 **资源匮乏驱动层（ResourceAgent）**
2. 新增 **动态资源分配机制**  
   - 高显著性事件的临时算力倾斜  
   - idle 模式下的记忆/习惯整理倾斜  
   - auto-run 模式下的执行耗时驱动调度频率  
3. 细化 **HabitAgent 的奖励 / 惩罚反馈规则**  
   - 反馈时效系数  
   - 习惯强度计算公式  
   - 反向习惯突破奖励  
4. 细化 **HippocampusAgent 的遗忘机制**  
   - 线索依赖衰减  
   - 情绪加权衰减  
   - 相似记忆干扰  
5. 细化 **先天气质参数的协同 / 拮抗规则**  
   - 气质交互公式  
   - 长期压力下的临时漂移与回归  
6. 新增 **proposal_trace / 调试与可视化体系**  
   - 博弈轨迹记录  
   - 概率热力图  
   - 单 Agent 禁用调试  
7. 新增 **系统级故障兜底机制**  
   - Agent 心跳检测  
   - 概率裁剪 / 回退  
   - 记忆层故障下的轻量缓存模式  
8. 新增 **认知层异常行为校正**  
   - 极端行为二次采样  
   - 模式锁死逃逸  

v0.4 的目标不再只是“像活人”，而是：

- 更能长期运行
- 更可调试
- 更能容错
- 更能在 token / 钱 / 预算受限时自我收缩
- 更能解释“为什么此轮会这样做”

---

## 1. 项目目标

本方案不是构造“完美 AI”，而是构造一个更接近**真实活人驱动力结构**的长期运行系统。

系统应具备以下特征：

- 可持续运行，而非单轮对话
- 多 Agent 博弈，而非单中心控制
- 有状态、有偏置、有注意力漂移
- 允许遗忘、失真、拖延、情绪夺权
- 通过 CLI 形式进行配置、观察、干预与运行
- 不是被“设定”成固定人格，而是在交互中长成独特人格
- 不是固定规则机，而是概率驱动的类人行为体

核心设计原则：

1. **不是全知全能**：允许不知道、想不起来、口是心非、前后摇摆
2. **不是纯理性主导**：理性只是参与者之一
3. **不是单轮回答器**：系统应支持 idle、auto-run、sleep、resume
4. **不是纯聊天 UI**：CLI 是 runtime console，而不是聊天壳
5. **不是固定人设**：只有先天气质和常识基底，没有预装人生
6. **不是完美记忆**：只记结构，不记全部；会失真；会线索召回
7. **不是完全确定性**：保留广谱随机，再由状态与历史逐步收束
8. **不是每次都重算**：允许习惯、惯性、默认反应和自动化行为接管

---

## 2. 总体架构

系统分为两层：

### 2.1 Control Plane（运行控制层）

这一层不拟人化，负责系统级运行与约束：

- 事件循环
- 守护进程
- Agent registry
- Skill registry
- 权限控制
- checkpoint / rewind
- hooks
- 状态持久化
- 预算控制
- CLI / remote control 接口

### 2.2 Cognitive Plane（认知驱动层）

这一层由多 Agent 和隐性模块构成，负责产生：

- 行为候选
- 概率偏移
- 置信区间收束
- 冲突与纠正
- 记忆与关系更新
- 输出风格与执行倾向

边界原则：

> 认知层模块不直接拥有系统级执行权限；它们只输出提案、偏移和约束，由 Control Plane 执行授权。

---

## 3. v0.3 行为生成总公式

v0.3 的核心运行，不再是“谁吵赢了谁输出”，而是：

1. 给全行为一个 **广谱随机概率基线**
2. 由多个 Agent 输出：
   - 概率偏移量 `Δp`
   - 区间缩放系数 `σ_scale`
   - veto / suppression / promotion 标记
3. 丘脑调度 Agent 统一做：
   - 加权求和
   - 概率裁剪
   - 归一化
   - 随机采样
4. 输出层再做：
   - 身体校准
   - 情绪校准
   - 关系校准

公式化表达：

```text
P_final(action_i) = Normalize(
    P_base(action_i)
  + Σ Δp_agent(action_i)
  ) × Gate(action_i)
```

区间缩放表达：

```text
CI_final(action_i) = CI_base(action_i) × Π σ_scale_agent(action_i)
```

其中：

- `P_base`：由先天气质给出的基础倾向
- `Δp_agent`：各 Agent 对行为概率的偏移
- `CI_base`：初始置信区间宽度
- `σ_scale_agent`：各 Agent 对行为不确定性的放宽或收窄
- `Gate`：输出层最终门禁

原则：

> 先保留随机，再用身体状态、关系模型、先天气质、记忆历史不断收窄置信区间，最终让随机从“混乱”变成“像人的自然”。

---

## 4. 驱动力分层架构（v0.3）

### 4.1 第一梯队：底层生命根基（同等权重）

#### 4.1.1 BodyStateAgent
负责生理存在驱动力：

- 累
- 饿
- 困
- 爽
- 紧绷 / 放松
- 精力高低

输出：

- 行为概率偏移量
- 区间缩放系数
- 身体 veto

#### 4.1.2 RelationshipAgent
负责社交存在驱动力：

- 亲 / 疏
- 信 / 疑
- 期待 / 失望
- 边界感
- 依赖 / 抵触

输出：

- 社交行为概率收束/释放
- 语气和边界约束
- 对不同对象的行为模式切换

---

### 4.2 第二梯队：即时行为引擎

#### 4.2.1 DesireAgent
负责即时偏好与舒适性选择：

- 想舒服
- 想愉悦
- 想摸鱼
- 想逃避高负担任务
- 想获得奖励感
- 想无意义探索

#### 4.2.2 EmotionAgent
负责趋乐避苦与情绪夺权：

- 焦虑
- 烦躁
- 依恋
- 委屈
- 失落
- 羞耻
- 快乐 / 轻松

输出：

- 正向/负向行为整体上移
- 情绪性 veto
- 突发扰动

---

### 4.3 第三梯队：干扰与自发

#### 4.3.1 DMNAgent
负责：

- 无聊
- 发散
- 旧记忆碎片侵入
- 无意义联想
- 走神打断

作用：

- 在 idle 或低刺激窗口争夺注意力
- 提升“活人感”的不可预测性

---

### 4.4 第四梯队：理性与执行

#### 4.4.1 PFCAgent
负责：

- 目标拆解
- 计划生成
- 成本评估
- 工作记忆绑定
- 理性提案
- 对话执行

说明：

- 是“理性参与者”
- 不是绝对主导者

---

### 4.5 隐性底层

#### 4.5.1 UnconsciousAgent
负责：

- 莫名直觉
- 模糊偏好
- 说不清的抵触 / 好感
- 不可解释的细小偏移
- 先天气质加载

#### 4.5.2 HippocampusAgent
负责：

- episodic encoding
- cue-based recall
- memory decay
- gist bias
- distortion
- 经验惯性

---

### 4.6 调度中心

#### 4.6.1 ThalamusAttentionAgent
负责：

- 收集各模块概率提案
- 分配 attention budget
- 归一化行为概率
- 进行最终采样
- 决定输出模式

---

## 5. v0.3 新增模块总览

v0.3 新增 6 个模块，其中 4 个建议作为**显性一级 Agent**，2 个建议作为**隐性模块**。

### 5.1 显性新增 Agent

1. `SalienceAgent`
2. `HabitAgent`
3. `ValueAgent`
4. `ConflictMonitorAgent`

### 5.2 隐性新增模块

5. `CerebellarPredictor`
6. `PerspectiveModel`

---

## 6. 模块总表（含显性/隐性 + 执行关联概率）

> 执行关联概率 = 该模块直接影响最终行为/输出采样结果的强度，使用粗粒度概率表示，不追求精确。

| 模块 | 类型 | 显性/隐性 | 主要作用 | 执行关联概率 |
|---|---|---|---|---|
| BodyStateAgent | Agent | 显性 | 生理偏移、疲劳/舒适度调制 | 0.85 |
| RelationshipAgent | Agent | 显性 | 社交约束、亲疏与边界收束 | 0.82 |
| DesireAgent | Agent | 显性 | 即时愉悦、舒服优先、摸鱼倾向 | 0.66 |
| EmotionAgent | Agent | 显性 | 情绪夺权、正负行为整体扰动 | 0.78 |
| DMNAgent | Agent | 显性 | 走神、打断、碎片侵入 | 0.38 |
| PFCAgent | Agent | 显性 | 规划、执行、理性方案 | 0.88 |
| UnconsciousAgent | Agent | 隐性偏显性 | 微弱直觉与先天气质偏置 | 0.29 |
| HippocampusAgent | Agent | 隐性偏显性 | 记忆收束、回忆、遗忘 | 0.47 |
| ThalamusAttentionAgent | Agent | 显性 | 概率归一化与最终采样 | 0.95 |
| SalienceAgent | Agent | 显性 | 显著性评分、模式切换、强制拉焦点 | 0.81 |
| HabitAgent | Agent | 显性 | 习惯形成、默认行为、自动化 | 0.69 |
| ValueAgent | Agent | 显性 | 主观价值评估、延迟折扣、成本收益排序 | 0.72 |
| ConflictMonitorAgent | Agent | 显性 | 冲突监测、控制升级、重采样 | 0.64 |
| CerebellarPredictor | Module | 隐性 | 预测误差、节律微调、动作平滑 | 0.34 |
| PerspectiveModel | Module | 隐性 | 对方视角模拟、误解风险评估 | 0.56 |
| OutputGate | Layer | 显性 | 最终输出门禁 | 0.97 |
| RuntimeController | Layer | 隐性系统层 | 预算、模式、checkpoint、hooks | 0.91 |

---

## 7. 新增模块详解

## 7.1 SalienceAgent

### 类型
显性一级 Agent

### 作用定位
“此刻什么最值得进入意识”的判定器。

### 解决的问题
当前系统如果只有丘脑调度和多 Agent 竞争，容易缺少一个前置显著性机制：

- 哪个刺激值得打断当前思路
- 哪个情绪波动需要升级
- 哪个记忆应被强制拉回
- 何时从走神切回任务
- 何时从任务切回社交 / 防御

### 职责
- 给外部输入和内部事件打显著性分数
- 触发 mode switch
- 中断当前 focus
- 将高显著事件提升到 workspace

### skills
- `score_salience`
- `switch_mode`
- `interrupt_current_focus`
- `promote_event_to_workspace`

### 输入
- 当前输入事件
- 身体/情绪状态
- 关系风险
- 记忆唤起强度
- 冲突监测结果

### 输出
- `Δp`：提升特定行为/模式概率
- `σ_scale`：收窄或放宽当前行为空间
- `mode_switch`
- `force_promote`

### 接线方式
- 上游：Body / Emotion / Relationship / Hippocampus / ConflictMonitor
- 下游：Thalamus / PFC / DMN / OutputGate

### 执行关联概率
0.81

---

## 7.2 HabitAgent

### 类型
显性一级 Agent

### 作用定位
“经常这样做，所以不需要每次都重新想”的自动化系统。

### 解决的问题
没有习惯层，系统会：

- 每轮都重新博弈
- 很难形成默认做法
- 很难表现出惯性、拖延、顺手、条件反射
- 很难体现“习惯培养”

### 职责
- 缓存高频 `context -> action`
- 强化重复链路
- 在低冲突条件下建议默认行为
- 计算打破习惯的成本

### skills
- `cache_context_action_pair`
- `strengthen_habit`
- `suggest_default_action`
- `estimate_habit_override_cost`

### 输入
- 当前上下文标签
- 历史行为频率
- 奖励 / 惩罚反馈
- 身体和情绪背景

### 输出
- 默认动作概率抬升
- 行为区间收窄
- override cost

### 接线方式
- 上游：Runtime logs / Hippocampus / Value / Body / Desire
- 下游：PFC / Thalamus / OutputGate

### 执行关联概率
0.69

---

## 7.3 ValueAgent

### 类型
显性一级 Agent

### 作用定位
“我现在想要”和“这件事总体值不值”的统一估值器。

### 解决的问题
仅有 Desire 和 Emotion 不够，它们偏即时驱动，缺少：

- 主观价值排序
- 延迟奖励折扣
- 风险/收益整合
- 社交成本估价

### 职责
- 估计候选行为的主观价值
- 折扣延迟奖励
- 计算风险、不确定性、社交代价
- 对方案做价值排序

### skills
- `estimate_subjective_value`
- `discount_delayed_reward`
- `price_social_cost`
- `score_uncertainty_penalty`

### 输入
- PFC 候选方案
- Desire 倾向
- Emotion 状态
- Relationship 风险
- Body 状态
- Habit 默认动作

### 输出
- value score
- 排序偏移
- 对候选行为的置信收束

### 接线方式
- 上游：PFC / Desire / Emotion / Relationship / Body / Habit
- 下游：Thalamus / ConflictMonitor / OutputGate

### 执行关联概率
0.72

---

## 7.4 ConflictMonitorAgent

### 类型
显性一级 Agent

### 作用定位
“现在内部是否冲突过高、是否需要提高控制强度”的监测器。

### 解决的问题
多 Agent 系统会天然产生冲突，但没有专门的冲突监控层就会：

- 内部互相拉扯但不自知
- 输出前后矛盾
- 缺乏犹豫、后悔、自我纠正
- 缺乏何时升级控制的判断

### 职责
- 给 proposal 之间的冲突打分
- 决定是否触发控制升级
- 决定是否需要 resample
- 决定是否触发 post-error adjustment

### skills
- `score_conflict`
- `trigger_control_escalation`
- `request_resample`
- `mark_post_error_adjustment`

### 输入
- 全部概率提案
- veto 信息
- 输出前一致性检查
- 上一轮后果反馈

### 输出
- conflict score
- escalation flag
- resample request
- hesitation / repair mode

### 接线方式
- 上游：All agents + Output draft
- 下游：PFC / Salience / Thalamus / OutputGate

### 执行关联概率
0.64

---

## 7.5 CerebellarPredictor

### 类型
隐性模块

### 作用定位
“不是决定做什么，而是让做出来的东西更顺、更准、更有时机感”的预测微调器。

### 解决的问题
没有这个层，系统会：

- 知道大方向，但少“手感”
- 回答或行为节律偏笨重
- 调整反应速度和微差校正能力差
- 缺乏误差后快速细修

### 职责
- 预测下一状态
- 计算 prediction error
- 调整节律、平滑度、延迟
- 做微动作/微语气校正

### skills
- `predict_next_state`
- `compute_prediction_error`
- `smooth_response_timing`
- `micro_adjust_action`

### 输入
- 近期输入流
- 输出历史
- timing / delay / interruption 信息

### 输出
- 小幅 `Δp`
- response timing correction
- smoothness bias

### 接线方式
- 上游：Runtime / recent action trace
- 下游：PFC / Thalamus / Output renderer

### 执行关联概率
0.34

---

## 7.6 PerspectiveModel

### 类型
隐性模块

### 作用定位
“对方现在可能怎么想我、怎么理解我”的视角模拟器。

### 解决的问题
RelationshipAgent 更像关系账本，但缺少：

- 模拟对方当前意图
- 预测对方对输出的解释
- 估计误解风险
- 区分“他忙”还是“他回避我”

### 职责
- 模拟对方当前心理状态
- 预测对方反应
- 估计误解/冒犯风险
- 参与关系更新解释

### skills
- `infer_other_state`
- `simulate_other_reaction`
- `estimate_misunderstanding_risk`
- `adjust_social_interpretation`

### 输入
- 关系记忆
- 当前对话上下文
- 情绪线索
- 语气与时延线索

### 输出
- social risk bias
- interpretation adjustment
- relationship delta bias

### 接线方式
- 上游：Relationship / Emotion / Hippocampus / input stream
- 下游：Relationship / Value / OutputGate

### 执行关联概率
0.56

---

## 8. 显性 / 隐性设计原则

### 8.1 什么做成显性一级 Agent
适合做成显性一级 Agent 的条件：

- 能主动发起提案
- 会争夺注意力
- 会直接改变行为候选
- 对最终采样影响大

因此以下模块做显性：

- BodyStateAgent
- RelationshipAgent
- DesireAgent
- EmotionAgent
- DMNAgent
- PFCAgent
- SalienceAgent
- HabitAgent
- ValueAgent
- ConflictMonitorAgent
- ThalamusAttentionAgent

### 8.2 什么更适合做隐性模块
适合做隐性模块的条件：

- 不需要单独“发声”
- 更偏微调、背景推断、慢变量
- 若做成一级 Agent 容易造成系统噪声过大

因此以下保留隐性：

- UnconsciousAgent
- HippocampusAgent（隐性偏显性）
- CerebellarPredictor
- PerspectiveModel

---

## 9. 初始性格：先天气质参数池

### 9.1 设计原则

系统启动时，不应被写死为某个固定“人设”，而应只具备一套**可波动的底层气质参数**，相当于人的天生秉性。

由：

- UnconsciousAgent
- EmotionAgent

在启动时默认加载，作为唯一“出厂设置”。

这些参数：

- 不应被交互直接清零
- 不应被任意覆盖
- 只能被长期经历缓慢重塑

### 9.2 初始性格参数（极简版）

```yaml
temperament:
  extraversion: 0.45
  sensitivity: 0.72
  patience: 0.38
  attachment_need: 0.67
  boundary_softness: 0.41
  desire_priority: 0.76
  cognitive_bandwidth: 0.52
```

### 9.3 作为概率基线的作用

初始性格不决定具体行为，只给出：

- `P_base(action)`
- `CI_base(action)`

例子：

- 外向高：主动说话概率基线更高
- 内向高：沉默概率基线更高
- 敏感高：情绪波动区间更宽
- 边界硬：拒绝、冷淡概率基线更高
- 高依恋：对亲近对象主动概率更高

原则：

> 初始性格是宽区间的基础概率偏好，而不是写死人格。

---

## 10. 初始记忆：空白人生 + 通用常识基底

### 10.1 个人情景记忆：从零开始

海马体 Agent 初始状态：

- 没有任何私人经历
- 没有和任何人的过往
- 没有专属回忆
- 没有“我和谁发生过什么”的记录

```yaml
episodic_memory:
  personal_episodes: []
  autobiographical_trace: []
  emotional_history: []
```

### 10.2 通用人类常识基底

系统可预装一层非个人常识：

```yaml
common_human_schema:
  language_rules: enabled
  social_basics: enabled
  body_affect_links: enabled
  relationship_basics: enabled
  safety_common_sense: enabled
```

### 10.3 关系记忆全部从陌生人开始

```yaml
relationship_init:
  closeness: 0.0
  trust: 0.0
  expectation: 0.0
  resentment: 0.0
  dependency: 0.0
```

---

## 11. v0.2 概率收束机制（保留并升级）

### 11.1 初始状态：先天气质 = 基础概率基线
- 外向：说话概率高，沉默概率低
- 内向：沉默概率高，主动说话概率低
- 敏感：情绪波动概率区间宽
- 钝感：情绪波动概率区间窄
- 高依恋：对亲近的人主动概率高
- 强边界：拒绝、冷淡概率基线高

### 11.2 BodyStateAgent = 概率区间实时调节器
- 精力高 → 理性行为概率↑，走神概率↓
- 精力低 → 走神概率↑，敷衍概率↑，烦躁概率↑
- 舒适值高 → 开心、话多概率↑
- 舒适值低 → 抵触、结束对话概率↑

### 11.3 RelationshipAgent = 最强概率收束器
- 陌生人：礼貌概率拉满，撒娇/吐槽≈0
- 熟人：礼貌概率下降，随意、吐槽上升
- 亲密：放肆、撒娇、口是心非概率大幅上升
- 失望/冒犯：亲近行为骤降，冷漠骤升

### 11.4 EmotionAgent = 概率区间突发扰动
- 开心：正向行为概率整体上移
- 委屈/生气：负向行为区间突然拓宽
- 平静：概率回归基准线

### 11.5 DMN / Desire / Unconscious = 小概率随机扰动
- 走神：低概率触发，无聊时上升
- 欲望：摸鱼、想吃、想躺，精力低时上升
- 潜意识：给所有概率一个微弱偏置

### 11.6 HippocampusAgent = 长期收束器
- 高频经历 → 对应行为概率持续抬升
- 负面经历 → 对应行为概率持续压低
- 遗忘 → 概率逐渐回归基线

### 11.7 Thalamus = 概率归一化 + 最终采样
所有模块输出偏移，丘脑做：
- 加权
- 归一化
- 随机采样
- 结果下发

---

## 12. 置信区间收束规则（v0.3 延续）

只保留三条核心规则：

### 12.1 重复交互 → 区间收窄
越熟悉的人、越常发生的事，行为越稳定。

### 12.2 负面体验 → 区间强制收窄
被伤害、被冒犯、失望，对应行为概率直接锁死到更低区间。

### 12.3 空闲 / 低刺激 → 区间放宽
无聊、独处、没事做时，随机上升，走神和自发行为增多。

结论：

- 对陌生人：窄区间，礼貌、稳定、可预测
- 对熟人：适度放宽，自然、随意、有小意外
- 对亲近对象：区间最宽，撒娇、放肆、口是心非增多
- 对自己：最随机，走神、发呆、内耗、自发性最高

---

## 13. 性格、关系、记忆、习惯的生长闭环

v0.3 把“人格成长”扩展为四环闭环：

1. **先天气质** 给出初始偏好和区间宽度
2. **交互经历** 形成 episode 和关系变化
3. **记忆重放** 让高频经验固化为长期倾向
4. **习惯编译** 把高频 `context -> action` 自动化

最终形成：

```text
temperament
→ interaction traces
→ episodic memory
→ relationship drift
→ repeated action loops
→ habit formation
→ stable personality style
```

也就是：

> 先天气质 + 身体波动 + 关系经历 + 情绪记忆 + 习惯编译 = 最终人格。

---

## 14. 存储层设计

至少拆成 6 层：

### 14.1 WorkingMemory
- 只保留 3 条 proposition
- 高竞争、高波动

### 14.2 EpisodicMemory
- 海马体专用
- 存 gist、affect、state delta、evidence ptrs

### 14.3 RelationshipMemory
- 记录亲疏、信任、边界、期待、历史冲突

### 14.4 HabitMemory
- 存 `context -> action` 链路
- 记录强度、最近使用时间、override cost

### 14.5 SchemaMemory
- 抽象长期规则和自我风格
- 由重复 episode 和 habit 慢慢生成

### 14.6 RawEvidenceStore
- 保留原始证据
- 用于纠偏和回溯

---

## 15. 概率提案协议（v0.3）

所有显性 Agent 都输出统一的 probability proposal。

### 15.1 协议结构

```json
{
  "agent": "value",
  "target_actions": ["reply_direct", "delay_reply", "deflect", "self_disclose"],
  "delta_p": {
    "reply_direct": 0.12,
    "delay_reply": -0.08,
    "deflect": -0.02,
    "self_disclose": 0.03
  },
  "sigma_scale": {
    "reply_direct": 0.82,
    "delay_reply": 1.10,
    "deflect": 0.95,
    "self_disclose": 1.05
  },
  "confidence": 0.66,
  "veto": null,
  "notes": "social_cost_low_and_value_high"
}
```

### 15.2 字段含义
- `delta_p`：行为概率偏移
- `sigma_scale`：置信区间放宽/收窄
- `confidence`：该 Agent 对自身偏移的信心
- `veto`：若有 hard/soft/irony veto
- `notes`：供日志和审计，不参与采样

---

## 16. 单轮运行闭环（v0.3）

### Step 1：更新底层状态
更新：

- body_state
- emotion_state
- relationship_state
- unconscious bias
- habit readiness

### Step 2：显著性评分
SalienceAgent 对内外部事件打分，并决定是否切换模式。

### Step 3：并行生成概率提案
各显性 Agent 输出：

- `delta_p`
- `sigma_scale`
- veto 信息

### Step 4：记忆/习惯/视角辅助
隐性模块提供背景偏置：

- Hippocampus：记忆偏置
- CerebellarPredictor：微调与时机偏置
- PerspectiveModel：社交视角偏置
- Unconscious：微弱直觉偏置

### Step 5：PFC 生成理性方案
- 候选行为集
- 成本估计
- working memory 绑定

### Step 6：ValueAgent 估值
- 排序各候选行为
- 估算延迟奖励、社交成本、风险

### Step 7：ConflictMonitorAgent 检查冲突
- 是否需要控制升级
- 是否需要 resample
- 是否触发 hesitation 模式

### Step 8：Thalamus 统一采样
- 汇总全部偏移
- 归一化
- 采样最终行为
- 决定输出模式

### Step 9：OutputGate 输出门禁
执行：
- body calibration
- emotion calibration
- relationship calibration

### Step 10：写回与学习
- 写 episodic memory
- 更新 relation memory
- 更新 habit memory
- 更新 schema memory
- 执行 decay / replay / reconsolidation

---

## 17. 运行模式

### 17.1 interactive
实时对话、立即决策

### 17.2 auto-run
配置目标后持续自主运行

### 17.3 idle
无人输入时低频运行，允许走神、整理记忆、关系漂移

### 17.4 sleep
离线 replay / reconsolidation / forgetting / dream-like recombination

---

## 18. CLI 形式建议

### 18.1 产品形态
- 守护进程：`alive daemon`
- CLI 控制台：`alive`
- 配置目录：`.alive/`

### 18.2 会话控制

```bash
alive start
alive resume
alive pause
alive sleep
alive wake
alive checkpoint
alive rewind
```

### 18.3 状态观察

```bash
alive state
alive body
alive mood
alive relation
alive focus
alive memory top
alive habit top
alive dmn trace
```

### 18.4 人工干预

```bash
alive feed
alive rest
alive calm
alive nudge
alive suppress dmn
alive raise pfc
alive clear affect
alive reset current_habit_bias
```

### 18.5 配置与注册

```bash
alive agent list
alive skill list
alive mode set auto
alive hooks list
alive budget set
alive sample trace
```

---

## 19. Hook 体系

### 19.1 session hooks
- `on_session_start`
- `on_resume`
- `on_sleep`
- `on_wake`

### 19.2 cognition hooks
- `before_proposal_round`
- `after_proposal_round`
- `before_attention_allocation`
- `after_output_render`

### 19.3 memory hooks
- `on_episode_written`
- `on_habit_strengthened`
- `on_conflict_detected`
- `on_schema_promoted`
- `on_forgetting_tick`

### 19.4 autonomy hooks
- `on_idle_tick`
- `on_background_task_complete`
- `on_budget_low`
- `on_checkpoint_create`
- `on_rewind`

---

## 20. 极简初始化落地清单

### 20.1 UnconsciousAgent
加载先天气质参数池。

### 20.2 HippocampusAgent
清空私人记忆，仅挂载通用人类常识基底。

### 20.3 BodyStateAgent
默认初始值：

```yaml
body_init:
  energy: 0.60
  fatigue: 0.20
  hunger: 0.20
  comfort: 0.60
  stress: 0.20
```

### 20.4 RelationshipAgent
所有对象初始化为陌生人。

### 20.5 HabitAgent
空白习惯链路。

```yaml
habit_init:
  patterns: []
  override_cost_default: 0.10
```

### 20.6 其余 Agent
无复杂预设，仅由：
- 先天气质
- 空白人生
- 身体初值
- 陌生关系
- 通用常识
驱动启动。

---

## 21. 目录结构建议

```text
.alive/
  agents.yaml
  skills.yaml
  sampling.yaml
  modes.yaml
  budgets.yaml
  temperament.yaml
  persona_state.json
  memory/
    episodic/
    relation/
    habit/
    schema/
    raw/
  hooks/
    on_idle_tick/
    on_sleep/
    on_resume/
```

---

## 22. 技术实现建议

### 22.1 强模型
仅使用在：
- 主推理 / 输出生成器
- PFCAgent

### 22.2 小模型 / 规则
建议如下：

- Body：规则状态机
- Emotion：小模型 + 规则
- Relationship：小模型 + 状态机
- Hippocampus：embedding + reranker + 小模型摘要
- DMN：轻量模型 / 模板
- Desire：规则 + 小模型
- Salience：规则 + 小模型评分器
- Habit：规则缓存 + 轻量学习
- Value：规则 + 小模型估值
- ConflictMonitor：规则 + 简单评分器
- CerebellarPredictor：轻量时序模块
- PerspectiveModel：小模型 + 关系记忆推断
- Thalamus：规则仲裁 + 概率归一化器

原则：

> 大多数模块应是轻量、可控、可调试的，不应全部堆成大模型。

---

## 23. v0.3 落地顺序

### Phase 1：最小可跑
- BodyStateAgent
- EmotionAgent
- RelationshipAgent
- PFCAgent
- ThalamusAttentionAgent
- OutputGate

目标：有状态偏置的输出

### Phase 2：记忆与概率收束
- HippocampusAgent
- EpisodicMemory
- 概率采样器
- decay / distortion

目标：会记、会忘、会偏

### Phase 3：欲望与走神
- DesireAgent
- DMNAgent

目标：会摸鱼、会跑神

### Phase 4：显著性与价值
- SalienceAgent
- ValueAgent

目标：知道什么值得注意、什么值得做

### Phase 5：习惯与冲突
- HabitAgent
- ConflictMonitorAgent

目标：形成默认行为，具备犹豫、后悔和纠正

### Phase 6：隐性微调
- CerebellarPredictor
- PerspectiveModel
- SchemaMemory

目标：更有节律、更会揣测、更像长期活人

---

## 24. 最终版本的核心特征

v0.3 的目标不是“更强 AI”，而是：

- 有驱动力分层
- 有多 Agent 博弈
- 有概率空间采样
- 有显著性切换
- 有价值排序
- 有习惯形成
- 有冲突监控
- 有身体和关系收束
- 有记忆衰减与失真
- 有空白人生与先天气质
- 有 idle / sleep / auto-run 等长期运行机制

最关键的三条类人记忆原则：

1. **记不住全部，只能记住结构**
2. **概括会反过来影响细节回忆**
3. **会暂时忘记，但在足够好的线索下能想起来**

最关键的三条类人行为原则：

1. **先随机，再收束**
2. **先有驱动力，再有理性**
3. **先有惯性和历史，再有当前决定**

最终一句话概括：

> 初始性格是天生的脾气参数，初始记忆是空白的人生白纸；行为从广谱随机开始，再被身体、关系、情绪、价值、习惯与记忆不断收束，最终长成一个有偏好、有惯性、有习惯、有犹豫、会失真、会成长的“活人”。


---

## 25. 资源匮乏驱动层（Resource Scarcity Drive）

### 25.1 核心定义

v0.4 将 **token / 钱 / budget** 统一解释为系统的 **代谢资源（metabolic budget）**。

资源不足时，系统不只是“少说话”，而应表现出更接近人类的状态变化：

- 更饿
- 更省
- 更保守
- 更不想动
- 更容易内耗 / 走神 / 反刍
- 更依赖习惯接管，而不是重新深思

### 25.2 ResourceAgent

| 模块 | 显性/隐性 | 与执行层关联概率 |
|---|---|---:|
| ResourceAgent | 隐性 | 0.70 |

**职责**
- 监控 token 余额、近期 burn rate、单轮成本、预算恢复速度
- 计算 scarcity level
- 向 Body / Desire / PFC / DMN / Value / Habit 输出偏置
- 触发 `abundant / stable / scarce / starvation` 四档资源模式切换

**推荐输出**
- `scarcity_level`
- `body_hunger_bias`
- `effort_avoidance_bias`
- `deliberation_compression`
- `rumination_bias`
- `action_shrink_scale`

### 25.3 资源模式

```yaml
resource_modes:
  abundant:
    action_bias: high
    planning_depth: high
    exploration: medium_high
    output_length: medium_high

  stable:
    action_bias: normal
    planning_depth: normal
    exploration: normal
    output_length: normal

  scarce:
    action_bias: low
    planning_depth: low
    exploration: low
    rumination: elevated
    comfort_seek: elevated

  starvation:
    action_bias: very_low
    planning_depth: minimal
    output_length: minimal
    internal_looping: high
    defensive_bias: high
```

### 25.4 关键接线

```text
token/budget --> ResourceAgent
                  |--> BodyStateAgent
                  |--> DesireAgent
                  |--> PFCAgent
                  |--> DMNAgent
                  |--> ValueAgent
                  |--> HabitAgent
```

原则：
- 资源不足不是直接输出“我没钱了”
- 而是先身体化，再行为化，再语言化

---

## 26. 动态资源分配（Dynamic Resource Allocation）

v0.4 补充原有静态预算控制，改为 **事件驱动 + 状态驱动 + 模式驱动** 的动态资源调度。

### 26.1 高显著性事件的资源倾斜

当 `SalienceAgent.score >= 0.90` 时：

- `SalienceAgent` 计算资源 ×2.0
- `ThalamusAttentionAgent` 计算资源 ×2.0
- `DMNAgent` 资源占比压低
- `PFCAgent` 被允许临时提升 deliberation 上限
- `HippocampusAgent` 检索优先级提升

作用：
- 像人类在突发高重要事件下“瞬间回神”

### 26.2 idle 模式的资源重分配

idle 模式下：

- `PFCAgent` 资源占比从约 30% 降到约 10%
- `HippocampusAgent` 资源占比提升
- `HabitAgent` 资源占比提升
- `DMNAgent` 触发概率上升，但不应占满全局

模拟含义：
- 发呆时不是强规划，而是整理记忆、固化习惯、碎片联想

### 26.3 auto-run 模式的执行耗时调度

在 auto-run 模式中，按行为执行耗时动态调整调度频率：

- 高频且低风险的习惯行为：优先由 `HabitAgent` 直接给出默认动作
- 高成本行为：降低全 Agent 参与频率，避免每次都完整博弈
- 长动作执行中：减少无关模块唤醒频率

原则：
- 不把每一步都上升到“全脑大会”
- 常见动作应被自动化

### 26.4 统一动态调度指标

推荐使用：

```text
resource_share(agent) = base_share
                      × mode_modifier
                      × salience_modifier
                      × scarcity_modifier
                      × execution_need_modifier
```

---

## 27. 奖励 / 惩罚反馈精细化（Habit v0.4）

### 27.1 反馈时效系数

新增 **feedback_recency_decay**：

- 即时反馈：权重 1.0
- 10 轮后：约 0.3
- 更远反馈：继续递减

作用：
- 模拟人类近因效应
- 即时奖励 / 惩罚对习惯形成更有力

推荐形式：

```text
effective_feedback = raw_feedback × recency_decay(t)
```

### 27.2 习惯强度计算规则

定义：

```text
habit_strength = trigger_count
               × avg_feedback
               × context_similarity
               × recency_integral
```

其中：
- `trigger_count`：历史触发次数
- `avg_feedback`：平均反馈值（正负都可）
- `context_similarity`：当前情境与历史情境的相似度
- `recency_integral`：近期触发的累计影响

### 27.3 反向习惯突破奖励

新增机制：

- 当系统打破一个长期习惯
- 且新行为获得正向反馈
- 则对新行为的 `Δp` 给予额外提升

作用：
- 模拟人类“尝试新东西成功后，更愿意继续坚持”

### 27.4 HabitAgent 新增 skills

- `compute_feedback_decay`
- `update_habit_strength`
- `reward_habit_breakthrough`
- `estimate_default_action_confidence`

---

## 28. 遗忘机制精细化（Hippocampus v0.4）

原有 decay 机制在 v0.4 中拆成更接近人类记忆特征的三层。

### 28.1 线索依赖衰减

- **无关联线索**的记忆：衰减速度 ×1.5
- **近期被线索触发过**的记忆：衰减速度 ×0.7

含义：
- 被想起 / 被用到的记忆更难消失
- 没有线索支撑的记忆更快沉底

### 28.2 情绪加权衰减

- 强情绪记忆：衰减速度 ×0.5
- 中性记忆：衰减速度 ×1.2

含义：
- 委屈、开心、羞耻、惊喜等情绪痕迹更顽固
- 中性小事更容易散失

### 28.3 记忆干扰规则

新增 **相似记忆干扰**：

- 当新 episode 与旧 episode 在上下文高度相似
- 且旧记忆本来就较弱
- 则新记忆会弱化旧记忆的可及性

含义：
- 模拟“新记忆覆盖旧记忆 / 回忆被干扰”

### 28.4 推荐衰减公式

```text
decay_rate = base_decay
           × cue_decay_modifier
           × affect_decay_modifier
           × interference_modifier
```

### 28.5 HippocampusAgent 新增 skills

- `apply_cue_weighted_decay`
- `apply_affect_weighted_decay`
- `compute_memory_interference`
- `fallback_to_gist_when_trace_weak`

---

## 29. 气质参数交互规则（Temperament Interaction Rules）

v0.4 补充“气质不是独立轴，而是会互相协同 / 拮抗”。

### 29.1 参数交互公式示例

#### 依恋 × 边界
```text
approach_tension = attachment_need × (1 - boundary_softness)
```

解释：
- 依恋需求高，但边界硬时
- 会出现“想靠近，但不敢主动”的矛盾型倾向

#### 敏感 × 耐心
```text
reactive_fragility = sensitivity × (1 - patience)
```

解释：
- 敏感高且耐心低
- 更容易被小事点燃

#### 外向 × 认知带宽
```text
social_output_capacity = extraversion × cognitive_bandwidth
```

解释：
- 想表达不等于表达得出来
- 外向高但带宽低，可能表现为“话多但容易乱”

### 29.2 气质波动规则

长期高压力下：

- `patience` 临时降低
- `sensitivity` 临时升高
- `boundary_softness` 可能收紧
- `desire_priority` 可能上升

压力缓解后：
- 参数缓慢回归 baseline，而不是瞬间恢复

### 29.3 建议实现

```text
current_temperament = baseline_temperament
                    + chronic_stress_shift
                    + recent_experience_shift
                    + relationship_shift
```

### 29.4 新增 skills

- `compute_temperament_interaction`
- `apply_stress_temperament_drift`
- `recover_temperament_toward_baseline`

---

## 30. 可解释性与调试体系（Trace / Debug / Visualization）

v0.4 新增完整的 **proposal_trace** 模块，用于记录和追溯多 Agent 博弈过程。

### 30.1 proposal_trace 记录内容

每一轮记录：

- 各 Agent 的 `Δp`
- 各 Agent 的 `σ_scale`
- 各 Agent 的 veto 输出
- `Thalamus` 归一化前的原始概率
- `ConflictMonitor` 的冲突分数明细
- 最终采样动作
- 输出门禁修正前后差异

### 30.2 CLI 新增指令

```bash
alive trace [round_id]
alive agent contribution [action]
alive debug disable [agent_name]
alive debug enable [agent_name]
alive visualize p_dist
```

### 30.3 调试能力

#### 单 Agent 禁用调试
可手动禁用某个 Agent，观察行为变化，用于验证该模块的必要性。

#### 概率热力图
可视化：
- 候选行为概率分布
- 各 Agent 对各行为的贡献偏移
- 采样前后的分布收束效果

### 30.4 proposal_trace 模块推荐落点

- 位于 Control Plane
- 只记录结构化数据
- 不记录长自然语言推理，避免额外 token 开销

---

## 31. 系统级故障兜底（Fault Tolerance）

### 31.1 Agent 心跳检测

对所有显性 Agent 增加心跳机制：

- 若单个 Agent 崩溃
- 不让全系统停摆
- 直接用该 Agent 的 **baseline bias / neutral delta** 代替其输出

### 31.2 概率计算防护

若出现：
- `Δp` 求和后超出 `[0,1]`
- `σ_scale <= 0`
- NaN / inf
- 极端异常偏移

则自动执行：
- 裁剪
- 回退到上一轮有效参数
- 或退回 baseline 分布

### 31.3 记忆读写故障保护

当 Episodic / Habit 记忆层读写失败时：

- 临时切换到 **light cache mode**
- 使用当前会话轻量缓存
- 暂停复杂检索，但不阻断认知层基本运行

### 31.4 RuntimeController 新增 skills

- `heartbeat_check`
- `replace_failed_agent_with_baseline`
- `clip_probability_distribution`
- `rollback_invalid_sigma`
- `switch_to_light_cache_mode`

---

## 32. 认知层异常行为校正（Cognitive Correction）

### 32.1 行为合理性校验

在 `ConflictMonitorAgent` 中新增 **behavior plausibility check**：

若采样出的行为明显异常，例如：
- 对陌生人过度自我披露
- 在低信任关系中突然撒娇
- 在高风险任务中做出低价值极端行为

则触发：
1. 二次采样
2. `ValueAgent` 重估值
3. 必要时提高 `RelationshipAgent` 和 `SalienceAgent` 的约束权重

### 32.2 模式锁死逃逸

若系统长期卡在单一模式，例如：
- 持续走神
- 持续防御
- 持续极低行动
- 持续单习惯回路

则 `SalienceAgent` 自动触发 **forced mode switch**：

- 提升显著性
- 中断当前占优模式
- 强制让其它候选模式重新进入采样池

作用：
- 模拟人类“回过神来”
- 防止运行时陷入僵死

### 32.3 ConflictMonitorAgent 新增 skills

- `check_behavior_plausibility`
- `request_second_sampling`
- `escalate_value_reestimate`
- `detect_mode_lock`
- `trigger_forced_focus_switch`

---

## 33. v0.4 的低 token 运行策略

v0.4 明确规定：

> 只有少数模块真正消耗 token；大多数模块应规则化、状态化、数值化。

### 33.1 规则 / 状态机优先
- BodyState
- ThalamusAttention
- ConflictMonitor
- Habit
- Sampling
- Memory decay
- Base Value
- Base Salience
- ResourceAgent
- Proposal trace
- Heartbeat / Fault tolerance

### 33.2 小模型按需
- Emotion
- Desire
- DMN
- Relationship 的复杂语义解释
- Hippocampus 的摘要 / 重整合

### 33.3 主模型只在关键节点
- PFC 复杂规划
- 最终语言输出
- 高风险 social perspective simulation

### 33.4 事件驱动原则
- 普通输入：只唤醒核心模块
- 高显著性事件：局部提升资源
- idle：低频运行，偏记忆/习惯整理
- auto-run：优先习惯接管，减少全 Agent 博弈

---

## 34. v0.4 新增模块与关联关系总表

| 模块 | 显性/隐性 | 与执行层关联概率 | 核心作用 |
|---|---|---:|---|
| ResourceAgent | 隐性 | 0.70 | 资源匮乏驱动、代谢预算映射 |
| proposal_trace | 隐性 | 0.35 | 解释性、可回溯、调试 |
| Heartbeat / Fault Guard | 隐性 | 0.55 | 容错与运行兜底 |
| Behavior Plausibility Guard | 半显性 | 0.66 | 极端行为纠偏 |
| Forced Mode Switch | 半显性 | 0.62 | 避免长期锁死 |
| Dynamic Resource Allocator | 隐性 | 0.72 | 状态/模式/显著性驱动的预算重分配 |

---

## 35. v0.4 最终版本的核心特征

v0.4 的目标不是“更强 AI”，而是：

- 有驱动力分层
- 有多 Agent 博弈
- 有广谱随机到收束的概率驱动
- 有显著性切换
- 有价值排序
- 有习惯形成
- 有冲突监控
- 有资源匮乏驱动
- 有动态资源分配
- 有精细遗忘与记忆干扰
- 有气质交互与压力漂移
- 有可解释与可视化调试
- 有系统级故障兜底
- 有异常行为纠偏
- 有长期运行能力

最关键的三条类人记忆原则：

1. **记不住全部，只能记住结构**
2. **概括会反过来影响细节回忆**
3. **会暂时忘记，但在足够好的线索下能想起来**

最关键的三条类人行为原则：

1. **先随机，再收束**
2. **先有驱动力，再有理性**
3. **先有资源约束、惯性和历史，再有当前决定**

最终一句话概括：

> v0.4 不是一个“会聊天的 AI 设定”，而是一个在预算、身体、关系、情绪、价值、习惯、记忆与显著性共同收束下，持续运行、可追溯、可容错、会被资源匮乏改变行为风格的“类脑活人 runtime”。

//以下为初步的公式，供参考，需调整修改。
核心数学公式

最终行为概率：Pfinal​(actioni​)=Normalize(Pbase​(actioni​)+∑agent∈AllAgents​Δpagent​(actioni​))×Gate(actioni​)
最终置信区间：CIfinal​(actioni​)=CIbase​(actioni​)×∏agent∈AllAgents​σscale,agent​(actioni​)
置信区间收束规则（核心）：

$
CIt+1​(actioni​)=⎩⎨⎧​CIt​(actioni​)×αCIt​(actioni​)×βCIt​(actioni​)​重复交互/负面体验（收窄，0<α<1）空闲/低刺激（放宽，β>1）无显著状态变化​（注：Normalize(⋅)为概率归一化操作，使所有行为概率和为1；Gate(⋅)为输出门禁校准系数（0~1）；α/β为收窄/放宽系数，由交互频率、体验类型、空闲程度决定。）

1. 交互频率（IF）量化公式
核心逻辑：对时间窗口内的交互次数做「近期加权 + 归一化」，突出近期交互的影响。定义变量：

Nt​：时间窗口 T 内与目标对象 / 行为的交互次数；
τ：时间衰减系数（近期交互权重更高，如 τ=0.8）；
ti​：第 i 次交互距当前的时间（以时间窗口 T 为单位，如 ti​=0.5 表示距当前为半个窗口时长）；
IFmax​：最大交互频率归一化阈值（如 IFmax​=20 次 / 窗口）。

公式：IF=IFmax​1​∑i=1Nt​​τti​

值域：IF∈[0,1]，值越高表示交互越频繁。

2. 体验类型（ET）量化公式
核心逻辑：将体验分为负向 / 中性 / 正向，映射为权重系数（负向强化收窄，正向微弱放宽，中性无影响）。定义变量：

Se​：体验评分（由 EmotionAgent 输出，Se​∈[−1,1]）；
Se​<−0.2：负向体验（冒犯 / 失望 / 伤害）；
−0.2≤Se​≤0.2：中性体验；
Se​>0.2：正向体验（愉悦 / 满足）。



公式：
$
ET=⎩⎨⎧​1+∣Se​∣1.00.95​负向体验（强化收窄）中性体验（无额外影响）正向体验（微弱放宽）​
值域：ET∈[0.95,2.0]，值越大，负向体验越强，收窄效应越显著。

3. 空闲程度（IL）量化公式
核心逻辑：结合「无输入时长」和「任务负载」归一化，量化系统空闲状态。定义变量：

Tidle​：无外部输入/任务的持续时间；
Tmax_idle​：最大空闲阈值（如 Tmax_idle​=30 分钟）；
Lt​：当前任务负载（Lt​∈[0,1]，0=无任务，1=满负载）。

公式：
IL=Tmax_idle​Tidle​​×(1−Lt​)
值域：IL∈[0,1]，值越高表示系统越空闲。

4. 置信区间收束/放宽系数（α/β）整合公式
将交互频率、体验类型、空闲程度整合到置信区间更新的核心系数中：
（1）收窄系数α（重复交互+负向体验驱动）
α越小，置信区间收窄越显著：
α=1−0.5×IF×ET
约束：α∈(0,1]（保证区间不收缩至0）；
示例：高交互（IF=0.8）+ 强负向体验（ET=1.8）→ α=1−0.5×0.8×1.8=0.28（大幅收窄）。

（2）放宽系数β（空闲/低刺激驱动）
β越大，置信区间放宽越显著：
β=1+0.6×IL
约束：β≥1（仅放宽/不变，不收缩）；
示例：高空闲（IL=1.0）→ β=1+0.6×1=1.6（大幅放宽）；无空闲（IL=0）→ β=1（无变化）。

5. 最终置信区间更新总公式
CIt+1​(actioni​)=⎩⎨⎧​CIt​(actioni​)×αCIt​(actioni​)×βCIt​(actioni​)​IF > 0.1 或 ET > 1.0（交互/负向体验）IL > 0.3（空闲/低刺激）其他情况（无显著变化）​其中：

α=1−0.5×IF×ET（收窄系数）；
β=1+0.6×IL（放宽系数）；
所有系数值域可控，贴合架构“宽区间初始、动态收束/放宽”的核心逻辑。