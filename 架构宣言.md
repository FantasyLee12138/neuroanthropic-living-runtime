# NALR 1.5 架构宣言

## 核心立场

NALR 不是为了兼容生态而牺牲内核结构的普通 agent 框架。`1.5` 的目标是在保持类脑概率运行时完整性的前提下，把系统做成真正可交付的产品形态。

兼容层、客户端、服务端、部署脚本、工作台都必须服务于内核完整性，而不能反过来规定 NALR 如何思考、如何记忆、如何形成自我和边界。

## 1.5 不可退让项

- 不能破坏单一概率真相面
- 不能让 observer、terminal、dashboard、launcher 维持多套事实
- 不能让 compatibility layer 绕过 trace、policy、safe mode、approval、repair
- 不能把外部学习做成无痕旁路
- 不能为了“能跑”而把系统降级成规则拼装壳

## 1.5 交付方向

- **本地客户端**
  允许用桌面壳承载现有 workbench，但必须统一调用同一服务状态和同一 runtime 主链。
- **单实例自托管**
  允许服务端常驻运行，但必须保留可探测、可停止、可重启、可回滚的管理面。
- **受控自主学习**
  网络与文件权限要以 policy surface 的方式进入系统，而不是临时脚本或越权访问。
- **验收优先**
  1.5 的主线是“有证据的可用性”，不是概念性机制的无止境扩张。

## 受控学习 doctrine

- 网络权限只能在 allowlist 域名和可观测来源上开放
- 文件权限只能在指定工作目录、学习目录和缓存目录内开放
- 学习行为必须留下 trace 并接受 budget、breaker、safe mode 和 rollback 约束
- 学习产物不能直接污染 canonical identity 或 runtime truth

## 工程判断标准

如果某项改动同时满足以下条件，它才符合 1.5：

- 增强用户可达性或系统长期运行能力
- 不削弱 TLH 的主体化与概率自治
- 能在 acceptance matrix 中被验证
- 失败时能够被诊断、抑制、熔断或回滚
