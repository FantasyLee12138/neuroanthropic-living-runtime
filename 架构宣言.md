# NALR 1.6 架构宣言

## 核心判断

`1.6` / `Chat Kernel V2` 的目标是把聊天主路径改成稀疏默认、异常加深。

普通回合不应该再走旧的全量多 agent 仪式。默认应该是一次 `CognitivePacket` 调用，加上代码里的确定性预算、约束、merge、写回和收束；只有在记忆线索、工具需求、冲突、不确定性或恢复对齐显著上升时，才逐层加深到 `chat_standard` 或 `chat_deep`。

## 不可退让项

- 单一权威状态不能丢。
- 历史只能通过 state、habit、relation、obligation 因果地影响未来，不能回退成模板人格。
- observer、Workbench、terminal、launcher 必须读同一份 route-first / module-first 真相。
- 状态修改必须由模型提议 `StatePatch`，再由代码验证后落到权威状态。

## 交付原则

- 代码负责 bookkeeping，模型只保留语义判断、答复草稿、是否需要深化的建议。
- 默认路径只读 `hot`；`warm/cold` 必须由 cue 或 activation 触发。
- consolidation 进入后台 job，不阻塞首 token。
- 公开合同优先展示 `activation_set`、`memory_tiers_read`、`packet_summary`、`background_jobs`、`deepen_reason`，而不是“哪些 agent 发言了”。

## 当前结论

- V2 路由语义已经落到 `chat_micro | chat_fast | chat_standard | chat_deep`。
- 公开 memory tier 已统一成 `hot / warm / cold`。
- 外部壳层已经切到 module 模型绑定，而不是 agent 模型绑定。
- 仍保留的旧 agent-tier 逻辑只允许存在于迁移兼容层，不能再作为产品主语义。

## 文档边界

当前根目录只保留三份活文档：[`README.md`](/Users/fantasylee/类脑架构/README.md)、[`架构宣言.md`](/Users/fantasylee/类脑架构/架构宣言.md)、[`PLAN1.6.md`](/Users/fantasylee/类脑架构/PLAN1.6.md)。历史材料统一从 [`archive/backup-docs/INDEX.md`](/Users/fantasylee/类脑架构/archive/backup-docs/INDEX.md) 进入。
