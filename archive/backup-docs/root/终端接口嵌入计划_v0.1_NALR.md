# 终端接口嵌入计划 v0.1

## 目标

把“类 Claude Code”的终端 coding assistant 嵌入现有 NALR 项目，作为**人机交互主入口**，而不是另起一套独立 runtime。

最终形态：

- **用户入口：`NALR`**
- **开发/运维入口：`alive`**
- **Node.js + TypeScript + Ink** 负责终端交互壳
- **Python NALR runtime** 继续负责状态、记忆、工具循环、trace、guard、safe mode

---

## 核心原则

### 1. `NALR` 是用户入口，不再用 `alive term`
用户应像使用 Claude Code 一样，直接在终端输入：

```bash
NALR
```

或：

```bash
NALR "帮我检查这个仓库的 bug"
```

也可以在项目目录内直接启动交互式 coding assistant。

### 2. `alive` 保留为开发/运维入口
`alive` 继续承担：

- trace 查看
- state / agents / skills / gates 调试
- checkpoint / rewind / safe mode
- observer / diagnostics / budget / operator commands

也就是：

- `NALR` 面向普通使用者
- `alive` 面向开发者和运维

### 3. Node 只做终端交互，Python 继续做大脑
不要在 Node 里再实现一套 agent/runtime。  
正确分工是：

- **Ink UI**：输入框、流式输出、tool call 展示、确认弹窗、slash commands
- **Bridge**：Node ↔ Python 会话协议
- **Python runtime**：tool loop、state、memory、guard、trace、审批后执行

---

## 目标架构

```text
NALR (Node + Ink)
   ↓
terminal bridge (session / event stream)
   ↓
Python NALR runtime
   ↓
tools / trace / memory / safe mode / observer
```

---

## 最小落地方案

### Phase 1：先做 `NALR` 入口壳

新增一个用户命令：

```bash
NALR
```

实现能力：

- 聊天式终端界面
- 流式输出
- 展示工具调用
- 对 `write_file` / `run_shell` 做确认
- 支持 slash commands：
  - `/help`
  - `/clear`
  - `/config`
  - `/model`

这一阶段先不改核心认知架构，只把现有 Python runtime 接到新终端壳上。

### Phase 2：把 coding tools 接进 Python runtime
首批工具全部注册到 Python 侧，而不是 Node 本地执行：

- `read_file(path)`
- `write_file(path, content)`
- `search_code(query)`
- `run_shell(command)`
- `git_status()`
- `git_diff()`

规则：

- `read/search/git_*` 默认可执行
- `write_file/run_shell` 必须审批
- 所有工具调用都写入 trace

### Phase 3：形成双入口产品结构

最终命令分工：

#### 用户层
```bash
NALR
NALR "修复这个函数的类型错误"
NALR "总结这个仓库结构"
```

#### 开发/运维层
```bash
alive trace round <id>
alive state show
alive skill stats
alive checkpoint create
alive safe on
```

这样做可以把“普通用户入口”和“开发者入口”真正分层。

---

## 目录改造建议

```text
repo/
├─ src/nalr/                    # 现有 Python runtime，保留
├─ services/observer/           # 现有 observer，保留
├─ apps/
│  └─ terminal/                 # 新增：NALR 终端前端（Node + Ink）
│     ├─ package.json
│     ├─ tsconfig.json
│     └─ src/
│        ├─ index.tsx
│        ├─ tui/
│        ├─ bridge/
│        ├─ commands/
│        └─ state/
├─ services/
│  └─ terminal_bridge/          # 新增：Node ↔ Python 协议层
└─ src/nalr/tools/coding/       # 新增：coding assistant tools
```

---

## 通信协议建议

优先使用 **stdio JSON event stream**，不要先上 HTTP。

### Node → Python
```json
{"type":"start_session","session_id":"s1","cwd":"/repo"}
{"type":"user_turn","session_id":"s1","text":"帮我分析这个报错","scenario":"task"}
{"type":"approve","session_id":"s1","call_id":"tool-4","approved":true}
```

### Python → Node
```json
{"type":"assistant_token","delta":"我先检查一下"}
{"type":"tool_call","call_id":"tool-2","tool":"read_file","args":{"path":"src/app.py"}}
{"type":"approval_request","call_id":"tool-4","tool":"run_shell"}
{"type":"tool_result","call_id":"tool-2","ok":true,"content":"..."}
{"type":"assistant_final","message":"问题在这里"}
```

---

## 必须坚持的边界

### 1. 工具执行必须在 Python runtime 内完成
Node 不直接执行 shell / write_file。  
否则会绕开 trace、审批、rollback、safe mode。

### 2. `NALR` 不重做一套状态系统
状态、记忆、慢变量、审批、trace 必须继续归属 Python runtime。

### 3. `NALR` 默认运行在 `task` 场景
因为 coding assistant 本质上就是高约束、强工具调用、低 DMN 的任务模式。

---

## 最终执行顺序

### P0
- 增加 `NALR` 命令入口
- 完成 Ink 终端壳
- 接上 Python bridge
- 支持流式输出和审批弹窗

### P1
- 接入 6 个 coding tools
- 所有工具调用进入 trace
- slash commands 分成本地命令和 runtime 命令

### P2
- 把 `NALR` 做成默认用户入口
- `alive` 收缩为 operator / developer interface
- 后续再补 why/agents/skills/gates 的侧栏或调试抽屉

---

## 一句话结论

不是再做一个独立 CLI，而是：

**用 `NALR` 作为用户入口壳，用 Ink 提供 Claude Code 式交互体验；用现有 Python NALR runtime 继续承担真正的认知、工具、trace 与控制逻辑。**
