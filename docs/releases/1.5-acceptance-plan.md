# NALR 1.5 Acceptance Plan

## Goal

把 TLH runtime、workbench、terminal、remote terminal、launcher、feedback loop runtime、self-hosted service 和 controlled learning policy surface 收敛成一个可以发布的 `1.5`。

## Acceptance Principles

- 必须符合 TLH、架构宣言和 frozen substrate 的底线
- 必须对用户可达，不是代码存在即可
- 必须有自动化、脚本化或可复现证据

## Release Gate

- P0 / P1 完成率达到 90% 以上
- 没有阻塞级 fail
- local client、self-hosted service、acceptance docs 三条交付线都可用
- bounded equivalent long-run 已存在，但真实 24h soak 只有在完整跑完后才可把 self-hosted 长跑条目从 `partial` 提升为 `pass`

## Acceptance Layers

### P0 Delivery

- `Open_NALR_Workbench.command` 可双击打开
- `alive-observer start` 可拉起服务并暴露 `/service/status`
- workbench 的 state / why / why-not / initiative / monologue 可用
- 单实例服务端具备启动、停止、重启、日志和状态探测能力

### P1 Consistency

- terminal / remote-terminal / dashboard / observer API 收敛到同一 read-model
- autonomy / initiative / monologue 状态一致
- 公开 route contract、权限模型、文档口径一致
- trace / replay / diagnostics 可解释
- 六层 `感知 -> 记忆 -> 认知 -> 决策 -> 执行 -> 反馈` trace / replay / controls contract 一致，反馈层由 `FeedbackLoopRuntime` 闭合

### P2 Enhancements

- 长时 soak 与预算报告
- 桌面分发增强
- 计划性学习策略与运维增强

## Controlled Learning Gate

- 默认学习模式是 `guided-learn`
- observer settings 与 runtime state 都暴露学习模式、allowlist 域名、可写目录、知识目录、学习日志目录和 trace 开关
- 外部学习必须可被 observer 和 terminal 观测
- 任何越权、预算失控或异常都必须能被 safe mode / breaker / rollback 截断

## Evidence Sources

- `python3 scripts/acceptance_15.py`
- `python3 scripts/acceptance_15.py --run --group controlled-learning`
- `python3 scripts/local_client_packager.py dist`
- `tests/unit/test_release_docs.py`
- `tests/unit/test_release_acceptance_report.py`
- `tests/unit/test_local_client_packager.py`
- `tests/unit/test_acceptance_15_runner.py`
- `tests/integration/test_observer_learning_settings.py`
- `tests/unit/test_tlh_runtime.py`
- `tests/unit/test_probability_field.py`
- `tests/unit/test_runtime_controller.py`
- `tests/unit/test_trace_store.py`
- `tests/integration/test_observer_api.py`
- `tests/integration/test_observer_launcher.py`
- `tests/browser/test_observer_dashboard_smoke.py`
- `tests/unit/test_feedback_loop.py`
- `tests/unit/test_remote_terminal_static_contract.py`
- `scripts/self_hosted_24h_soak.py`
- `apps/terminal npm test`
- `apps/terminal npm run build`
