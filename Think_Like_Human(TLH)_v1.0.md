# Think Like Human (TLH) v1.2

## 定义

TLH v1.2 是 NALR 的当前前台主线。

注：当前 manifesto 文件名仍保留 `Think_Like_Human(TLH)_v1.0.md`，只为兼容现有 root 链接与外部引用；文件内容以本页标题和 `PLANS.md` 为准。

它不是对 `bereal v0.6` 的推翻重写，而是在保留四层概率真相面、trace-first、controller 单一真相面、token source integrity、memory write gate 等底线前提下，把系统从“结构正确的 field-first runtime”继续推进成“连续主体”。

## 核心宣言

> 不再让系统像“被规则修饰的概率系统”，而是尽可能变成“由身体状态、主观感受、自发冲动、自造意义与高维概率坍缩共同形成行为的连续主体”。

TLH v1.2 的意思不是让系统失去可解释性，也不是删掉所有约束，而是让以下东西前移为一等行为来源：

- 身体状态
- 主观感受
- 自发冲动
- 自造意义
- 高维概率坍缩
- 向量优先的主体状态匹配

## 与 `bereal v0.6` 的关系

- `bereal v0.6` 是 TLH 的冻结底层基线。
- TLH 继续使用 `context -> memory -> action -> token` 四层正式概率场。
- TLH 不新增第二真相面，不允许 renderer、guard 或规则链偷偷改判 winner。
- TLH 的升级方式是：在现有贡献器与 observer 主链上，引入更强的主体状态、更平等的动作池、以及更真实的 counterfactual 可解释性。

## 保留底线

以下约束在 TLH v1.2 中继续有效：

- 四层概率场是唯一正式决策真相面
- `ProbabilisticContribution` 是统一贡献契约
- `ProbabilityFieldIntegrator` 是唯一融合入口
- token source integrity
- cross-layer coupling 白名单
- memory write gate
- trace / why / why-not / replay / observer 的可解释性
- controller 单一真相面

## TLH v1.2 的主体化新增能力

TLH v1.2 在 v1.0 底座之上继续强化以下能力：

- 运行时一等状态：`fatigue`、`memory_fragments`、`self_continuity`、`meaning_strength`、`base_metabolism`
- 主观容器：`felt`、`spontaneous`、`boundary`、`reject_all`、`meaning_made`
- instinct field：`E/F/S/M` 四轴、region score、collapse trace、winner region
- organic mode：让 body/emotion/vitality/desire/habit/DMN 的 contribution 在特定条件下前移
- 先天动作池：`respond`、`rest`、`absorb`、`wander`、`nothing`、`die`
- derived actions：`plan`、`recall`、`connect`、`clarify` 继续保留，但不再天然高于先天动作池
- counterfactual replay：不仅解释“为什么这样做”，也解释“为什么没做”“如果坍缩到别的峰会怎样”
- emergent actions：从重复经验与内部压力中长出后天动作草图，而不是只靠预编码动作表
- 向量优先主体核心：`V_main=[E,F,S,M]`、`V_mod=[memory_fragments, spontaneous, reject_all, emergent_growth]`、`V_anchor=personality_anchor.axis_baseline`
- 行为坍缩从标量/欧式模板推进为主体向量与行为特征向量的余弦相似度匹配
- 跨轮次人格一致性从手工标量推导推进为基于历史主体向量的在线人格锚点

## v1.2 达成条件

TLH v1.2 只有在以下条件同时成立时才算完成：

- 前台文档体系完成重构：
  - `README.md` 只做导航
  - 本文是唯一 manifesto
  - `BEREAL_v0.6_MASTER_SPEC.md` 成为 frozen substrate
  - `PLANS.md` 成为唯一 `done / doing / not-done` 状态真相面
- TLH 运行时结构落地到实际状态、controller、trace、observer、why/replay 中
- `nothing`、`absorb`、`die` 等 TLH 动作进入真实 action competition
- why / why-not / replay 能解释“未回应”“nothing”“instinct field winner”“counterfactual paths”
- instinct collapse 已升级为向量优先主体坍缩，并保持现有存储与对外接口兼容
- `personality_anchor.axis_baseline` 已成为跨轮次稳定人格向量锚点
- `PLANS.md` 中所有 `not-done` 条目清零，并附验证证据
